# MNIST Classification with MobileNetV2

This project performs handwritten digit classification on the MNIST dataset using MobileNetV2 with transfer learning and fine-tuning.

## Setup Instructions

1. **Create the Conda environment**

Make sure you have Conda installed. Then run:

```bash
conda env create -f environment.yml
conda activate MNIST
```

2. **Run the notebook**

Open the notebook in Jupyter, VS Code, or your preferred IDE and run all cells in **Zvenyika_N_CSE347_Project.ipynb** to train and evaluate the model.
